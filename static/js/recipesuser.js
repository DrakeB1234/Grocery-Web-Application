// function for toggling settings nav
document.querySelectorAll(".recipe-card").forEach(e => {
    e.addEventListener("click", (e) => {
        // if next element in DOM is hidden
        console.log(e.target.parentElement)
    });
});